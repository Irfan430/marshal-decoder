# XPRO NEXUS DECODER - Requirements
# Optimized for Python 3.7-3.13

# Core decoding libraries (with version constraints for Python 3.13)
uncompyle6>=3.9.0,<4.0.0
decompyle3>=3.9.0,<4.0.0
xdis>=6.0.0,<7.0.0

# Alternative decompilers (Python 3.13 compatible)
pycdas==1.16.0
decompyle3==3.9.1

# Utility packages (must-have)
colorama>=0.4.0
tqdm>=4.65.0
psutil>=5.9.0
rich>=13.0.0

# Data processing (optional but recommended)
numpy>=1.24.0
pandas>=2.0.0

# For Python 3.13+ compatibility (bytecode analysis)
bytecode>=0.13.0
opcode>=0.0.1

# Lightweight alternatives
# Remove heavy ML packages that cause issues
# scipy>=1.10.0
# scikit-learn>=1.2.0
# torch>=2.0.0
# tensorflow>=2.12.0

# Development tools (optional)
black>=23.0.0
flake8>=6.0.0

# Security (optional)
bandit>=1.7.0